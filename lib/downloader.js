
var URL = require('url'),
			HttpDownloader = require('./httpdownloader'),
			http = require('http'),
	   fs = require('fs');

var Downloader = function( conf, url ) {
	
};

module.exports = Downloader;